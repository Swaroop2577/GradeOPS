# ── Core framework ──────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic>=2.7.4,<3.0.0
httpx>=0.27.0
python-multipart==0.0.9

# ── LangChain / LangGraph ───────────────────────────────────────────────────
langchain==0.3.25
langchain-core==0.3.58
langchain-google-genai==2.1.4
langgraph==0.3.34

# ── Google Gemini (OCR + grading LLM — free tier, 1500 req/day) ─────────────
google-genai>=0.8.0

# ── Sentence Transformers (plagiarism embeddings) ────────────────────────────
sentence-transformers==3.0.0

# ── PDF processing (pymupdf — no system dependencies, replaces pdf2image) ────
pymupdf>=1.24.0
Pillow>=10.3.0
pdfplumber==0.11.0

# ── Utilities ────────────────────────────────────────────────────────────────
numpy>=1.26.4
python-dotenv==1.0.1

# ── REMOVED ──────────────────────────────────────────────────────────────────
# openai              → replaced by google-genai (Gemini)
# langchain-openai    → replaced by langchain-google-genai
# pdf2image           → replaced by pymupdf (no Poppler needed)